// @flow
import React, { PureComponent } from "react";
type PropsType = $Exact<{}>;
type StateType = $Exact<{
  bla: string
}>;

class HomeScreen extends PureComponent<PropsType, StateType> {
  constructor(props: PropsType) {
    super(props);
  }
}
